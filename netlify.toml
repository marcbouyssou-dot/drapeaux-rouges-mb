[build]
command = """
git clone https://github.com/flutter/flutter.git --branch stable --depth 1
export PATH="$PATH:$(pwd)/flutter/bin"
flutter config --enable-web
flutter build web
dart run scripts/generate_pwa_service_worker.dart
"""

publish = "build/web"
